export '../../../features/gesture_control/gesture_control_screen.dart';

import '../../../features/gesture_control/gesture_control_screen.dart';

/// Alias used by [router.dart] and existing navigation paths.
class GestureScreen extends GestureControlScreen {
  const GestureScreen({super.key});
}
